server {
    listen 4200;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: unknown paths (e.g. /seller after a refresh) resolve to index.html.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
